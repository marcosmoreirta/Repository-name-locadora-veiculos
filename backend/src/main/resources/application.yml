server:
  port: 8087

spring:
  application:
    name: locadora-backend

  datasource:
    url: jdbc:postgresql://db.lfwnobgihdkskkazryzj.supabase.co:5432/postgres
    username: postgres
    password: o3goN2KmdxpfpgER
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect